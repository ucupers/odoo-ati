<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data>

    </data>

    <!-- User akses EPI -->
    <record model="ir.module.category" id="module_category_epi">
        <field name="name">EPI</field>
        <field name="description">Helps you manage your product development processes</field>
        <field name="sequence">7</field>
    </record>

    <record id="group_epi_user_readonly" model="res.groups">
		<field name="name">EPI / Read Only</field>
	</record>

     <record id="group_epi_user" model="res.groups">
		<field name="name">EPI / User</field>
        <field name="implied_ids" eval="[(4, ref('sis_epi.group_epi_user_readonly'))]"/>
	</record>

	<record id="group_epi_manager" model="res.groups">
		<field name="name">EPI / Manager</field>
         <field name="implied_ids" eval="[(4, ref('sis_epi.group_epi_user'))]"/>
	</record>


</odoo>
